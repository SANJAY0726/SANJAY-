# Deployment Links

- **GitHub repository**: https://github.com/SANJAY0726/SANJAY-
- **Vercel frontend**: https://product-recommendation-system.vercel.app
- **Render configuration**: `render.yaml` is configured for deployment in this repository.

## Notes

- The GitHub remote is configured in the `Product_Recommendation_system` repository.
- The Render deployment is set up with `gunicorn` in `requirements.txt` and `render.yaml`.
- If you want the actual Render service URL, connect this repo on Render and deploy the `product-recommendation-system` service.
